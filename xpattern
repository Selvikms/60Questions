include<stdio.h>
void main()
{
	int n,i,j;
	printf("enter n value:");
	scanf("%d",&n);
	for(i=1;i<n-1;i++)

		{
			for(j=1;j<=n;j++)
			{
				if((i==j) || (n==i+j+1))
					printf("%d",i);
				else
					printf("  ");
			}
			printf("\n");
		}

}
