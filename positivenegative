include<stdio.h>
#include<math.h>
int main()
{
	double n;
	printf("Enter the number: ");
	scanf("%lf",&n);

	if(n>0)

	printf("%lf if a positive number",n);

	else if(n==0)
	printf("%lf if zero",n);

	else
	printf("%lf is a negative number",n);

	return 0;
}
