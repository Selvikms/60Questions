#include<stdio.h>
int main()
{
  int length,breadth,area;
  printf("\nEnter the length and breadth:");
  scanf("%d%d",&length,&breadth);
  area=length*breadth;
  printf("\nArea of rectangle: %d",area);

    return 0;
}
